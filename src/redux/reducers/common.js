const defaultState = {
};
// TODO: add error field to state
// TODO: add user field to state
export default (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
